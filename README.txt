il faut executer le project.sql dans le dossier project/ avant de prendre le code
commande

CREATE DATABASE mit;
CREATE USER 'localhost'@'mit' identified by "123456";
mysql -u mit -p
source project.sql 
